"use client";

import { motion } from "framer-motion";
import { Check, CheckCircle2, FileImage, FileText, MessageSquare, RotateCcw, Search, Send, X } from "lucide-react";
import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";

type EvaluationStatus = "Pending" | "Approved" | "Rejected" | "Resubmission" | "Published";
type Submission = { id: string; student: string; enrollment: string; practical: string; subject: string; submitted: string; progress: number; status: EvaluationStatus; observations: { label: string; value: string }[]; files: { type: "pdf" | "image"; label: string }[] };

const initialSubmissions: Submission[] = [
  { id: "sub-001", student: "Aarav Mehta", enrollment: "CV-24-018", practical: "Acid Base Titration", subject: "Analytical Chemistry", submitted: "Today, 10:42 AM", progress: 92, status: "Pending", observations: [{ label: "Initial reading", value: "0.0 mL" }, { label: "Final reading", value: "24.6 mL" }, { label: "Endpoint", value: "Pale pink" }], files: [{ type: "pdf", label: "aarav-practical-record.pdf" }, { type: "image", label: "titration-observation-01.png" }] },
  { id: "sub-002", student: "Maya Sharma", enrollment: "CV-24-027", practical: "UV Spectroscopy", subject: "Physical Chemistry", submitted: "Today, 09:18 AM", progress: 78, status: "Pending", observations: [{ label: "Wavelength", value: "λmax configured" }, { label: "Absorbance", value: "0.742 AU" }, { label: "Sample ID", value: "UV-04" }], files: [{ type: "pdf", label: "maya-uv-record.pdf" }, { type: "image", label: "spectrum-plot.png" }] },
  { id: "sub-003", student: "Kabir Rao", enrollment: "CV-24-034", practical: "Water Hardness", subject: "Environmental Chemistry", submitted: "Yesterday, 04:26 PM", progress: 100, status: "Pending", observations: [{ label: "Trial 1", value: "18.2 mL" }, { label: "Trial 2", value: "18.0 mL" }, { label: "Result", value: "Within range" }], files: [{ type: "pdf", label: "kabir-water-hardness.pdf" }] },
];

const markFields = ["Practical Marks", "Viva Marks", "Observation Marks", "Calculation Marks"] as const;

export function TeacherEvaluation() {
  const [submissions, setSubmissions] = useState(initialSubmissions);
  const [query, setQuery] = useState("");
  const [selectedId, setSelectedId] = useState(initialSubmissions[0].id);
  const [marks, setMarks] = useState<Record<string, string>>({});
  const [remarks, setRemarks] = useState("");
  const [preview, setPreview] = useState<string | null>(null);
  const selected = submissions.find((submission) => submission.id === selectedId) ?? submissions[0];
  const filtered = useMemo(() => submissions.filter((submission) => `${submission.student} ${submission.practical} ${submission.subject}`.toLowerCase().includes(query.toLowerCase())), [query, submissions]);

  function updateStatus(status: EvaluationStatus) {
    setSubmissions((items) => items.map((submission) => submission.id === selected.id ? { ...submission, status } : submission));
  }

  return <div className="space-y-6 p-4 sm:p-6 lg:p-8"><div><Badge variant="outline" className="mb-3 bg-background">Teaching Workspace</Badge><h1 className="text-3xl font-bold tracking-tight">Teacher Evaluation</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">Review submitted practicals, evaluate evidence, and publish clear feedback for students.</p></div><div className="grid gap-4 lg:grid-cols-[minmax(18rem,0.8fr)_minmax(0,1.5fr)]"><Card className="h-fit"><CardHeader><CardTitle>Submitted Practicals</CardTitle><CardDescription>{filtered.length} submissions awaiting review.</CardDescription><div className="relative mt-2"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search students or practicals" className="pl-9" /></div></CardHeader><CardContent className="space-y-2">{filtered.map((submission, index) => <motion.button key={submission.id} type="button" initial={{ opacity: 0, x: -8 }} animate={{ opacity: 1, x: 0 }} transition={{ delay: index * 0.05 }} onClick={() => { setSelectedId(submission.id); setPreview(null); }} className={`w-full rounded-lg border p-3 text-left transition-colors ${selected.id === submission.id ? "border-primary bg-primary/5" : "bg-background hover:bg-muted/50"}`}><div className="flex items-start justify-between gap-2"><div><p className="text-sm font-medium">{submission.student}</p><p className="mt-1 text-xs text-muted-foreground">{submission.practical}</p></div><Badge variant="outline" className={submission.status === "Pending" ? "border-amber-500/30 text-amber-600" : "border-emerald-500/30 text-emerald-600"}>{submission.status}</Badge></div><div className="mt-3 flex items-center justify-between text-xs text-muted-foreground"><span>{submission.submitted}</span><span>{submission.progress}% complete</span></div></motion.button>)}{filtered.length === 0 && <p className="py-8 text-center text-sm text-muted-foreground">No submissions match the search.</p>}</CardContent></Card>

<div className="space-y-4"><Card><CardHeader><div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><CardTitle>{selected.student}</CardTitle><CardDescription>{selected.enrollment} · {selected.practical} · {selected.subject}</CardDescription></div><Badge variant="outline">{selected.submitted}</Badge></div><div className="mt-4"><div className="mb-2 flex justify-between text-sm"><span>Experiment Progress</span><span className="font-medium">{selected.progress}%</span></div><div className="h-2 rounded-full bg-muted"><div className="h-2 rounded-full bg-primary" style={{ width: `${selected.progress}%` }} /></div></div></CardHeader><CardContent className="space-y-5"><div><h3 className="mb-3 flex items-center gap-2 text-sm font-semibold"><CheckCircle2 className="h-4 w-4 text-primary" />Observation</h3><div className="grid gap-3 sm:grid-cols-3">{selected.observations.map((observation) => <div key={observation.label} className="rounded-lg border bg-background p-3"><p className="text-xs text-muted-foreground">{observation.label}</p><p className="mt-1 text-sm font-medium">{observation.value}</p></div>)}</div></div><div><h3 className="mb-3 flex items-center gap-2 text-sm font-semibold"><FileText className="h-4 w-4 text-primary" />Uploaded Evidence</h3><div className="flex flex-wrap gap-2">{selected.files.map((file) => <Button key={file.label} variant="outline" size="sm" onClick={() => setPreview(file.label)}>{file.type === "pdf" ? <FileText className="mr-2 h-4 w-4" /> : <FileImage className="mr-2 h-4 w-4" />}{file.label}</Button>)}</div>{preview && <div className="mt-3 flex min-h-28 items-center justify-center rounded-lg border border-dashed bg-muted/20 p-4 text-center text-sm text-muted-foreground">Preview placeholder for <span className="ml-1 font-medium text-foreground">{preview}</span></div>}</div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{markFields.map((field) => <label key={field} className="space-y-2"><span className="text-xs font-medium text-muted-foreground">{field}</span><Input type="number" min="0" max="100" placeholder="0" value={marks[`${selected.id}-${field}`] ?? ""} onChange={(event) => setMarks((current) => ({ ...current, [`${selected.id}-${field}`]: event.target.value }))} /></label>)}</div><label className="block space-y-2"><span className="flex items-center gap-2 text-sm font-medium"><MessageSquare className="h-4 w-4 text-primary" />Remarks</span><Textarea value={remarks} onChange={(event) => setRemarks(event.target.value)} placeholder="Add feedback for the student" /></label></CardContent></Card><Card><CardHeader><CardTitle>Evaluation Actions</CardTitle><CardDescription>Choose the result workflow for this submission.</CardDescription></CardHeader><CardContent className="flex flex-wrap gap-2"><Button variant="outline" onClick={() => updateStatus("Approved")}><Check className="mr-2 h-4 w-4" />Approve</Button><Button variant="destructive" onClick={() => updateStatus("Rejected")}><X className="mr-2 h-4 w-4" />Reject</Button><Button variant="outline" onClick={() => updateStatus("Resubmission")}><RotateCcw className="mr-2 h-4 w-4" />Return for Resubmission</Button><Button onClick={() => updateStatus("Published")}><Send className="mr-2 h-4 w-4" />Publish Results</Button></CardContent></Card></div></div></div>;
}
